Import('env')

tinyxml_sources = Split("""
	tinystr.cpp
	tinyxml.cpp
	tinyxmlerror.cpp
	tinyxmlparser.cpp""")

tinyxml_headers = Split("""
	tinyxml.h
	tinystr.h""")


tinyxmlenv = env.Copy()

# Set any platform specific options for this target
tinyxmlenv.UseTargetOptions('tinyxml')

tinyxml = tinyxmlenv.Library('tinyxml', tinyxml_sources)
tinyxmlenv.Install('$LIBDIR', tinyxml)

env.Distribute('libtinyxml', tinyxml_sources)
env.Distribute('libtinyxml', tinyxml_headers)
env.Distribute('libtinyxml', 'SConscript')

# For politeness
env.Distribute('libtinyxml', 'readme.txt')
env.Distribute('libtinyxml', 'changes.txt')

