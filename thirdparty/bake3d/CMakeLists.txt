PROJECT(BAKE3D)

# Internal dependencies
FILE(GLOB BAKE3D_SRCS ${PROJECT_SOURCE_DIR}/*.c ${PROJECT_SOURCE_DIR}/*.cpp )

# Internal dependencies
INCLUDE(${SHADERVM_SOURCE_DIR}/export.cmake)
INCLUDE(${AQSISTYPES_SOURCE_DIR}/export.cmake)

ADD_LIBRARY(bake3d SHARED ${BAKE3D_SRCS} ${INFORES_SRCS})

SET_TARGET_PROPERTIES(bake3d PROPERTIES OUTPUT_NAME "bake3d" SOVERSION ${MAJOR} VERSION "${MAJOR}.${MINOR}")

INSTALL(TARGETS bake3d
	RUNTIME DESTINATION ${SHADERDIR})

