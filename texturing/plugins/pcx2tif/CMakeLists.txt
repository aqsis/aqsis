PROJECT(PCX2TIF)

INCLUDE(${PLUGIN_COMMON_SOURCE_DIR}/local.cmake)
SET(PCX2TIF_SRCS ${PROJECT_SOURCE_DIR}/pcx2tif.c ${PLUGIN_COMMON_SRCS})

INCLUDE_DIRECTORIES(${AQSIS_TIFF_INCLUDE_DIR})

ADD_LIBRARY(pcx2tif SHARED ${PCX2TIF_SRCS})
TARGET_LINK_LIBRARIES(pcx2tif ${AQSIS_TIFF_LIBRARIES} ${AQSIS_ZLIB_LIBRARIES})

INSTALL(TARGETS pcx2tif 
	RUNTIME DESTINATION ${PLUGINDIR}
	LIBRARY DESTINATION ${PLUGINDIR}
	ARCHIVE DESTINATION ${PLUGINDIR}
	)

