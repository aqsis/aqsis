PROJECT(PNG2TIF)

INCLUDE(${PLUGIN_COMMON_SOURCE_DIR}/local.cmake)
SET(PNG2TIF_SRCS ${PROJECT_SOURCE_DIR}/png2tif.c ${PLUGIN_COMMON_SRCS})

INCLUDE_DIRECTORIES(${AQSIS_TIFF_INCLUDE_DIR})

ADD_LIBRARY(png2tif SHARED ${PNG2TIF_SRCS})
SET(LINKLIBS ${AQSIS_TIFF_LIBRARIES} ${AQSIS_ZLIB_LIBRARIES})
INCLUDE(${PPM2TIF_SOURCE_DIR}/link.cmake)
TARGET_LINK_LIBRARIES(png2tif ${LINKLIBS})

INSTALL(TARGETS png2tif 
	RUNTIME DESTINATION ${PLUGINDIR}
	LIBRARY DESTINATION ${PLUGINDIR}
	ARCHIVE DESTINATION ${PLUGINDIR}
	)

