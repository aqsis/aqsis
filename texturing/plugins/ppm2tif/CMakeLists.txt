PROJECT(PPM2TIF)

INCLUDE(${PLUGIN_COMMON_SOURCE_DIR}/local.cmake)
SET(PPM2TIF_SRCS ${PROJECT_SOURCE_DIR}/ppm2tif.c ${PLUGIN_COMMON_SRCS})

ADD_LIBRARY(ppm2tif SHARED ${PPM2TIF_SRCS})
TARGET_LINK_LIBRARIES(ppm2tif ${AQSIS_TIFF_LIBRARIES} ${AQSIS_ZLIB_LIBRARIES})

INSTALL(TARGETS ppm2tif 
	RUNTIME DESTINATION ${PLUGINDIR}
	LIBRARY DESTINATION ${PLUGINDIR}
	ARCHIVE DESTINATION ${PLUGINDIR}
	)

