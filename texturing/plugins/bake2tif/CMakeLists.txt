PROJECT(BAKE2TIF)

INCLUDE(${PLUGIN_COMMON_SOURCE_DIR}/local.cmake)
SET(BAKE2TIF_SRCS ${PROJECT_SOURCE_DIR}/bake2tif.c ${PLUGIN_COMMON_SRCS})

INCLUDE_DIRECTORIES(${AQSIS_TIFF_INCLUDE_DIR})

ADD_LIBRARY(bake2tif SHARED ${BAKE2TIF_SRCS})
TARGET_LINK_LIBRARIES(bake2tif ${AQSIS_TIFF_LIBRARIES} ${AQSIS_ZLIB_LIBRARIES})

INSTALL(TARGETS bake2tif 
	RUNTIME DESTINATION ${PLUGINDIR}
	LIBRARY DESTINATION ${PLUGINDIR}
	ARCHIVE DESTINATION ${PLUGINDIR}
	)

