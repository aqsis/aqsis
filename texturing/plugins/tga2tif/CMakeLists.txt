PROJECT(TGA2TIF)

INCLUDE(${PLUGIN_COMMON_SOURCE_DIR}/local.cmake)
SET(TGA2TIF_SRCS ${PROJECT_SOURCE_DIR}/tga2tif.c ${PLUGIN_COMMON_SRCS})

ADD_LIBRARY(tga2tif SHARED ${TGA2TIF_SRCS})
SET(LINKLIBS ${AQSIS_TIFF_LIBRARIES} ${AQSIS_ZLIB_LIBRARIES})
INCLUDE(${JPG2TIF_SOURCE_DIR}/link.cmake)
TARGET_LINK_LIBRARIES(tga2tif ${LINKLIBS})

INSTALL(TARGETS tga2tif 
	RUNTIME DESTINATION ${PLUGINDIR}
	LIBRARY DESTINATION ${PLUGINDIR}
	ARCHIVE DESTINATION ${PLUGINDIR}
	)

