######################################################################
# Render jobs
######################################################################

RenderJob("", "Bug1031110.rib", 
          description="""
<b>Bug 1031110: Some grayscale TIF images make Aqsis/teqser hang/crash.</b>
<p> 
A MakeTexture or teqser call ("teqser gray.tif out.tex") 
on the attached TIF file (320x240 grayscale image) 
results in the message:

Exceeding allocated texture memory by 1270792192

and Aqsis/teqser hangs (using up all available memory).

The image was generated by converting a GIF image 
using PIL. If I create a grayscale TIF directly in 
Photoshop the problem does not occur.

Aqsis 0.9.2 on WinXP.

""")
