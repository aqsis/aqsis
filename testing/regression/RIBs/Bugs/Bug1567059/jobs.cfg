######################################################################
# Render jobs
######################################################################

RenderJob("", "Bug1567059.rib", shaders=["liquidpoint.sl"],
          description="""
<b>Bug 1567059 : Colour issue when rendering Liquid(Maya) RIBs.</b>
<p>When rendering RIBs generated by 'LiquidForMaya7' (and
higher) objects appear to be the wrong colour in some,
not all, instances.

I have enclosed a Liquid-generated RIB for further
inspection, which should render a grey torus but
instead renders a yellow one.

My tests have been done with a fairly recent (SVN)
build of Aqsis, though I can confirm 3Delight, Air and
Pixie produce the correct result.  

""")
