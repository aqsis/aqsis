Import('env')
Import('pixelsave')
pcx2tif_sources=Split("""
	pcx2tif.c""")

pcx2tif = env.SharedLibrary('pcx2tif', pcx2tif_sources + pixelsave, LIBS=['$tiff_lib', '$z_lib'])

env.Install('$BINDIR', pcx2tif)
