Import('env')

slxargs_sources = Split("""
	rcdummy.cpp
	slx.cpp""")

slxargs_headers = Split("""
	slx.h""")

# Set any platform specific options for this target
env.UseTargetOptions('slxargs')

slxargs = env.Library('slxargs', slxargs_sources)
env.Install('$LIBDIR', slxargs)

env.Distribute('libslxargs', slxargs_sources)
env.Distribute('libslxargs', slxargs_headers)
env.Distribute('libslxargs', 'SConscript')
env.Distribute('libslxargs', 'Makefile.am')

