#!/bin/sh
command()
{
	file=$1

	echo "---------------------------------------------------------"
	echo "rendering $file" ... 
	echo "---------------------------------------------------------"
	./ribrenderer @top_srcdir@/ribber.cfg $1 
}

for file in @srcdir@/*.rib; do
	command $file
done

exit 0
