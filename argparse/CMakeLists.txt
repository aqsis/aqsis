PROJECT(ARGPARSE)

INCLUDE(${PROJECT_SOURCE_DIR}/export.cmake)

SET(ARGPARSE_SRCS argparse.cpp)
SET(ARGPARSE_HDRS argparse.h)
ADD_LIBRARY(argparse STATIC ${ARGPARSE_SRCS})

INSTALL(TARGETS argparse
	ARCHIVE DESTINATION ${STATICLIBDIR})